# Normalize line endings and mark binary files.
# Default: let Git detect text files and normalize them in the repository to LF.
* text=auto

# Force LF for common source and text files in the repo
*.sh     text eol=lf
*.py     text eol=lf
*.js     text eol=lf
*.ts     text eol=lf
*.css    text eol=lf
*.html   text eol=lf
*.md     text eol=lf
*.json   text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.rs     text eol=lf

# PowerShell scripts — keep CRLF on checkout if helpful on Windows (optional)
*.ps1    text eol=crlf

# Binary files
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.pdf    binary
*.zip    binary
*.exe    binary

# Ensure lock/db binary files are treated as binary
backend/database/data/*.mdb binary
